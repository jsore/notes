/**
 * ../databases/lib/parse-rdf.js
 *
 * Library to define parseRDF function
 */
'use strict';

/**
 *  assigning an anonymous function to module.exports, takes a
 *    single param (rdf) & returns nothing, but is till a function
 */
module.exports = rdf => {
};